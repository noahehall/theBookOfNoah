# shell
  ```sh
    # confirm one instance can access another
      ping OTHER_INSTANCE_NAME

    # ssh into another instance
      ssh INSTANCE_NAME

    # curl another instance
      curl http://INSTANCE_NAME/
  ```